import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  textArea: {
    height: 100,
  },
  iconSuccess: {
    width: 64,
    height: 64,
    borderRadius: 32,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
