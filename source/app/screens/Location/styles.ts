import {StyleSheet} from 'react-native';
import {Spacing} from '@passionui/components';

export default StyleSheet.create({
  locationButton: {position: 'absolute', bottom: Spacing.XL, right: Spacing.XL},
});
