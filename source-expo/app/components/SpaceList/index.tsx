import {SizedBox, Spacing} from '@passionui/components';
import React from 'react';

const SpaceList: React.FC = () => {
  return <SizedBox width={Spacing.M} height={Spacing.M} />;
};

export {SpaceList};
